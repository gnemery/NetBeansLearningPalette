<PaletteItem>
    <Category>Decisions</Category>
    <DisplayName>Else If</DisplayName>
    <Tooltip>This is an else if-statement, a conditional check in your code that only\n executes if the condition specified is true and\nabove if statements are false.\nAttach this to an existing IF statement!</Tooltip>
    <Example>if (1 + 1 == 3) { //False } else if ( 1 + 1 == 2) { //True }</Example>
    <Icon>org/qcc/modules/learningpalette/Decisions/component16.png</Icon>
    <Languages>
        <Language id="JAVA">
            <codeTemplate1>else if ($Expression) { $Comment
                //TODO: Your code here.
                }</codeTemplate1>
        </Language>
        
        <Language id="CPP">
            <codeTemplate1>else if ($Expression) { $Comment
                //TODO: Your code here.
                }</codeTemplate1>
        </Language>
    </Languages>
    <!-- Customizer dynamic creation at runtime, currently supported controls:
        Text, Combo
    -->
    <Customizer>
        <Item type="Expression" name="ConditionalValue" label="Conditional Value:" value="" />
        <Item type="Text" name="Comment" label="Comment:" value="" />
    </Customizer>
    <PaletteLogicClass>org.qcc.modules.learningpalette.Decisions.LP_ElseIf</PaletteLogicClass>
</PaletteItem>